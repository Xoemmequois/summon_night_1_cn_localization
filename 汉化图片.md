# 汉化图片（Image）说明

本文说明如何翻译 `pic_output\` 里的图片，对应 README 的「步骤 5：汉化图片」。
示例使用 **Photoshop**。

要翻译的图片在 `romhack\pic_output\` 下，每个 `.gif` 旁边都有同名的 `.act`（调色板）
和 `.gif.meta.json`（写回位置信息）。翻译好的图放到 `romhack\pic_output_translated\` 下
**相同的相对路径**即可。

---

## 一、操作流程（Photoshop）

1. **打开 GIF**：打开要汉化的 `.gif`。GIF 默认是**索引模式**。
2. **切成 RGB**：菜单 `图像 / 模式 / RGB 颜色`，把索引模式改为 RGB 颜色，才能正常编辑。
3. **编辑图片**：替换/修改其中的文字。**尽量使用与原图相同的颜色**。
   （原因见下面的「注意事项」：构建只回写像素索引，不会改游戏里的调色板。）
4. **改回索引**：编辑完成后，菜单 `图像 / 模式 / 索引颜色`。
5. **载入原始调色板**：在「索引颜色」对话框中，`调板` 选择 **自定义**，在弹出的对话框里
   选择 **载入**，选择**与 GIF 同名的 `.act` 文件**。
6. **设置透明色**：点击弹窗右边的**吸管图标**，然后点击颜色表**左上角第一个颜色**，
   把它设为透明色。（游戏内基本都是把第 0 个颜色用作透明色。）
7. **导出 GIF**：菜单 `文件 / 导出 / 存储为 Web 所用格式（旧版）`，格式选 **GIF**，导出。
8. **放到指定位置**：把导出的文件保存到下面的「保存位置」。

---

## 二、保存位置（对应位置说明）

原图在：

```
romhack\pic_output\<子目录>\<文件名>.gif
```

翻译后的图**必须**保存到：

```
romhack\pic_output_translated\<相同的子目录>\<相同的文件名>.gif
```

例如：

| 原图（`pic_output`） | 翻译后（`pic_output_translated`） |
| --- | --- |
| `romhack\pic_output\mapnames\mapname10.gif` | `romhack\pic_output_translated\mapnames\mapname10.gif` |
| `romhack\pic_output\misc\battle_ui.gif` | `romhack\pic_output_translated\misc\battle_ui.gif` |
| `romhack\pic_output\char_names\char_9_sub2_part_7.gif` | `romhack\pic_output_translated\char_names\char_9_sub2_part_7.gif` |

说明：

- **只需要汉化的图才用放**，不需要翻译的图不用复制过去。
- 文件名和相对路径必须和 `pic_output\` 里的一一对应；构建时按这个对应关系回写。
- 同名 `.act` 一起复制过去也可以，但**构建不会读取它**（构建只读 GIF 的像素索引），
  它主要是给你在 Photoshop 里载入调色板用的。
- `.gif.meta.json` **不需要**复制。构建写回时读取的是 `pic_output\` 里那份同名
  `.gif.meta.json`（记录写回位置），`pic_output_translated\` 下的 meta 不会被使用。

---

## 三、注意事项

- 必须是 **8bpp 索引色 GIF**，**尺寸与原图一致**。
- **构建只回写像素索引，不会回写游戏内的调色板**。所以：
  - 画面用的颜色必须是**原图调色板里已有的颜色**（用同色系的原色最保险）；
  - 不要引入调色板以外的全新颜色，否则游戏里仍会显示原来的颜色。
- **索引不能超出原图位深**：4bpp 的图最大索引是 15，8bpp 最大 255。用原调色板通常就不会越界。
- **索引 0 保持透明**（第 6 步那个左上角第一个颜色）。
- 第一次改完后，建议先跑一次 `build.bat`，看 `romhack\pic_output_preview\` 里的预览和
  `*_diff.png`（白色表示未改动区域），确认没问题再继续批量翻译。
