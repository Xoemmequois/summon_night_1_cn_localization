# 汉化对话（Dialog）说明

本文说明《Summon Night》对话文本的结构、`processed\` 的用法，以及几个已知的特殊情况。
对应 README 的「步骤 3：汉化文字（对话框）」。

- 译文维护位置：`ruby_tools\opencode_generated\processed\scriptXX_fidYY.txt`
- 合并工具：`apply_text.bat`（内部调用 `romhack\apply_translations.py`）
- 真正写回 ROM 用的文件：`romhack\zh_CN_translated.json`

---

## 一、基本单位是「行」

游戏显示对话时，**最小的单位是一行文本框**。在数据里，一行对应一个标签：

```
[FID:01, TEXT:0078] 前辈！
```

- `FID`：对话文件 ID（十六进制）。它和脚本 ID 相关，等价于 `zh_CN.json` 里 key 的前半段。
- `TEXT`：该对话文件里这一行的编号（十六进制）。
- `[FID:.., TEXT:..]` 合起来唯一定位一行文本。

若干行按顺序组成一个**组（Group）**，也就是一段对话：

```
--- Group [right:絵美 next=2012] ---G
せんぱぁい！
ちょっと、ちょーっと
待ってくださいよぉ！
+[FID:01, TEXT:0078] 前辈！
+[FID:01, TEXT:0079] 等一下，等一下，请等
+[FID:01, TEXT:007A] 我一下嘛！
```

- 组头里的 `[right:絵美]` / `[left:主人公]` 是说话人和站位（right=画面右侧，left=左侧）。
- 组名里的 `---G` 表示**这个组已经汉化好**；不带 `---G` 的组会被 `apply_translations.py` 忽略。

> 注意进制：`processed\` 里 `FID`/`TEXT` 是**十六进制**，而 `zh_CN.json` 的 key 是
> **十进制**的 `FID-TEXT`。例如 `[FID:01, TEXT:0078]` 对应 key `0001-0120`
> （`0x01`=1，`0x78`=120）。

---

## 二、同一行会被多个组复用

游戏的同一行文本（同一个 `FID`+`TEXT`）**会出现在多个组里** —— 不同场景复用同一句台词。
翻译时必须保证：**同一个 `TEXT` 行在所有组里的译文完全一致**。

真实样例（`processed\script01_fid01.txt`）：

```
[组 0] --- Group [right:絵美 next=2012] ---G
センパーイ！                     ← 原文（组 0）
ちょっと、ちょーっと
待ってくださいよぉ！
+[FID:01, TEXT:0078] 前辈！
+[FID:01, TEXT:0079] 等一下，等一下，请等
+[FID:01, TEXT:007A] 我一下嘛！

[组 20] --- Group [right:克也 next=2012] ---G
センパーイっ！                   ← 原文（组 20，开头那句不同）
ちょっと、ちょーっと
待ってくださいよぉ！
+[FID:01, TEXT:00A2] 前辈！
-[FID:01, TEXT:0079] 等一下，等一下，请等
-[FID:01, TEXT:007A] 我一下嘛！
```

可以看到：

- 组 0 与组 20 是两段不同场景，但**复用了同一行** `TEXT:0079`、`TEXT:007A`。
- 行首的 `+` / `-` 由 `process_translations.py` 生成：
  - `+` = 这一行在本文件里**第一次出现**；
  - `-` = 这一行**前面已经出现过**（复用），不要把它当成新行单独翻译。
- 开头那句分别是 `TEXT:0078`（组 0）和 `TEXT:00A2`（组 20），是**两行不同的文本**，所以是两个 `+`。

> 在 `processed\` 的全部 141 个文件里，共有约 **2941 个文本行**出现在不止一个组中，
> 所以「按行、且跨组一致」是常态而不是个例。

`apply_translations.py` 正是按 `(FID, TEXT)` 收集译文的：同一个 key 若在不同组里出现
不同译文，脚本会直接报错并要求你先解决冲突。

---

## 三、行数与长度限制

- 翻译流程是「**整组合并成一句 → 翻译 → 按原组行数拆回**」。拆回时：
  - 原文某行 **≤10 个字符**时，对应译文行也**不得超过 10 个字符**（游戏每行的显示上限）。
  - 行首不要出现逗号、句号、省略号、问号、感叹号等标点。
  - 原文没有逗号的地方，译文也不要加逗号（日文顿号「、」可以译成中文逗号「，」）。

---

## 四、几个已知的特殊情况

1. **FID68 可能没有被实际引用**：这部分对话在游戏中可能不会出现。
2. **FID76 中有部分内容没有在 `processed\` 下**：这些内容在游戏中本来就没有用到，
   它们是**埃尔卡的夜对话的后续**，看起来像是废案。
3. **FID0D 有一段原文是 5 行**：游戏中显示不全，我们已经把它改成了 4 行
   （多出来的那一行留空）。
4. **极少数「抽一行复用」的组会被去重优化丢掉**：生成 `processed\` 时，为了去除大量重复组，
   如果某个组里所有行都是 `-` 开头（即全部已在前面出现过），这个组就不再输出。但极少数对话
   是从其他组里**抽出一行**来单独复用、并**单独形成一个组**的 —— 例如 `FID:14, TEXT:0017`
   （原文 `いえ・・・`，译文 `……`）原本用在下面这组里（`script04_fid14.txt`）：

   ```
   --- Group [right:雷德 next=2012] ---G
   しかし、
   なりゆきだったとは
   いえ・・・
   +[FID:14, TEXT:0015] 但是，
   +[FID:14, TEXT:0016] 虽说这也是情势所迫
   +[FID:14, TEXT:0017] ……
   ```

   之后它又单独成一个组，接在下面这组之后：

   ```
   --- Group [right:雷德 next=2012] ---G
   やれやれ・・・
   身勝手な要求をして、
   すまないな
   +[FID:14, TEXT:006B] 哎呀哎呀……
   +[FID:14, TEXT:00A5] 提出这么任性的要求，
   +[FID:14, TEXT:00A6] 真是抱歉啊

   --- Group [right:char_0012 next=2012] ---G
   いえ・・・
   -[FID:14, TEXT:0017] ……
   ```

   形成了如下的对话：
   
   やれやれ・・・
   身勝手な要求をして、
   すまないな

   いえ・・・

   最后这个只含「いえ・・・」的组全是 `-`，去重时被删掉，于是这段对话在 `processed\`
   里就少了这一句。目前遇到的这种情况较少，处理方案是在实际游玩过程中发现后再特殊处理。

---

## 五、`processed\` 不一定包含全部待翻译文本

受静态提取的限制，可能会有 `zh_CN_translated.json` 里出现的文本没有出现在 `processed\` 下，
尤其是 **FID 较低**的文件 —— 这部分是早期提取的，一些后来才发现的提取技巧没有应用到它们上面。

这些内容在当前 git 版本里都已经汉化了。如果你实际游玩时发现这部分汉化有问题、需要修改，
可以直接改 `romhack\zh_CN_translated.json`。

